@namespace ValorantInfoApp.Features.Pages.Components.WeaponCard
@inject NavigationManager Navigation


<MudPaper Class="card align-center" Style="height: 95%;" @onclick="RedirectToPage">
    <MudImage Src="@PictureUri" Style="height: auto; width: 100%; margin-top: 1vw;" />
    <MudText Typo="Typo.h6">@Name</MudText>
</MudPaper>

@code
{
    [Parameter] public string PictureUri { get; set; } = null!;
    [Parameter] public string Name { get; set; } = null!;
    [Parameter] public Guid Id { get; set; }
    private void RedirectToPage()
    {
        Navigation.NavigateTo($"armory/{Id}");
    }
}
